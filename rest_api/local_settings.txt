DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'project_name',
        'USER':'user',
        'PASSWORD':'password',

    }
}

DEBUG = True
ALLOWED_HOSTS = ['*',]